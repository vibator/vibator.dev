import{H as e,ht as t,nt as n,tt as r}from"./chunks/framework.qcK96eCy.js";var i=JSON.parse(`{"title":"vibator.knip namespace","description":"","frontmatter":{},"headers":[],"relativePath":"reference/knip-namespace.md","filePath":"reference/knip-namespace.md"}`),a={name:`reference/knip-namespace.md`};function o(e,i,a,o,s,c){return t(),r(`div`,null,[...i[0]||=[n(`<h1 id="vibator-knip-namespace" tabindex="-1"><code>vibator.knip</code> namespace <a class="header-anchor" href="#vibator-knip-namespace" aria-label="Permalink to “vibator.knip namespace”">​</a></h1><p>This document is the reference of the <code>knip</code> subnamespace as <code>@vibator/knip</code> registers it onto the shared <code>vibator</code> object. Importing the package (or <code>src/namespace/knip.ts</code> directly) performs the registration; rules then reach it as <code>vibator.knip</code>.</p><p>The subnamespace drives Knip through its programmatic API (<code>knip</code> and <code>knip/session</code>). Knip decides whether code is unused from the whole workspace, so the namespace runs it once per configuration; callers intersect the issues with the files in scope.</p><h2 id="reference" tabindex="-1">Reference <a class="header-anchor" href="#reference" aria-label="Permalink to “Reference”">​</a></h2><ul><li><a href="#functions">Functions</a>: The calls the subnamespace exposes.</li><li><a href="#knipoptions">KnipOptions</a>: The options every call accepts.</li><li><a href="#knipissue">KnipIssue</a>: One issue with its file and position.</li><li><a href="#caching">Caching</a>: How analyses are reused.</li></ul><hr><h2 id="functions" tabindex="-1">Functions <a class="header-anchor" href="#functions" aria-label="Permalink to “Functions”">​</a></h2><table tabindex="0"><thead><tr><th>Declaration</th><th>Description</th></tr></thead><tbody><tr><td>configFile(options?: <a href="#knipoptions">KnipOptions</a>): string | undefined</td><td>The absolute path of the configuration file a run uses, or undefined when Knip discovers its own. Throws when a configured path names no file.</td></tr><tr><td>issues(options?: <a href="#knipoptions">KnipOptions</a>): Promise&lt;<a href="#knipissue">KnipIssue</a>[]&gt;</td><td>Analyzes the workspace and returns every issue Knip reports, flattened across Knip&#39;s buckets.</td></tr><tr><td>fix(options?: <a href="#knipoptions">KnipOptions</a>): Promise&lt;void&gt;</td><td>Runs Knip with its fixes enabled: unused exports and types are stripped and unused dependencies removed from <code>package.json</code>. Knip writes the files itself.</td></tr></tbody></table><h2 id="knipoptions" tabindex="-1">KnipOptions <a class="header-anchor" href="#knipoptions" aria-label="Permalink to “KnipOptions”">​</a></h2><p>The options every call accepts.</p><table tabindex="0"><thead><tr><th>Declaration</th><th>Description</th></tr></thead><tbody><tr><td>configPath?: string</td><td>Reference to a Knip configuration file: a <code>./</code> path from the project root, an absolute path, or a <code>package:path</code> reference. When omitted, Knip discovers its own configuration (<code>knip.json</code>, <code>knip.ts</code>, the <code>knip</code> field in <code>package.json</code>, …).</td></tr></tbody></table><h2 id="knipissue" tabindex="-1">KnipIssue <a class="header-anchor" href="#knipissue" aria-label="Permalink to “KnipIssue”">​</a></h2><p>One Knip issue with its file and position.</p><table tabindex="0"><thead><tr><th>Declaration</th><th>Description</th></tr></thead><tbody><tr><td>type: string</td><td>The issue bucket, such as <code>exports</code>, <code>files</code>, or <code>dependencies</code>.</td></tr><tr><td>filePath: string</td><td>The absolute path of the file the issue points at.</td></tr><tr><td>symbol: string</td><td>The symbol the issue is about: an export, a dependency, a specifier.</td></tr><tr><td>parentSymbol?: string</td><td>The enclosing symbol, for enum and class members.</td></tr><tr><td>line?: number</td><td>The 1-based line, when Knip resolves a position.</td></tr><tr><td>col?: number</td><td>The 1-based column, when Knip resolves a position.</td></tr><tr><td>fixable: boolean</td><td>Whether Knip can remove the issue itself under a fix run.</td></tr></tbody></table><p>The fixable buckets are <code>exports</code>, <code>types</code>, <code>dependencies</code>, and <code>devDependencies</code>.</p><h2 id="caching" tabindex="-1">Caching <a class="header-anchor" href="#caching" aria-label="Permalink to “Caching”">​</a></h2><p>One analysis runs per configuration path and is reused across calls for the duration of the run. A <code>fix</code> call invalidates the cache, so the next <code>issues</code> call reanalyzes.</p>`,17)]])}var s=e(a,[[`render`,o]]);export{i as __pageData,s as default};