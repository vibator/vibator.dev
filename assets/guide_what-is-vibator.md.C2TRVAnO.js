import{V as e,et as t,mt as n,tt as r}from"./chunks/framework.DPTLmtS5.js";var i=JSON.parse(`{"title":"What is Vibator?","description":"","frontmatter":{},"headers":[],"relativePath":"guide/what-is-vibator.md","filePath":"guide/what-is-vibator.md"}`),a={name:`guide/what-is-vibator.md`};function o(e,i,a,o,s,c){return n(),t(`div`,null,[...i[0]||=[r(`<h1 id="what-is-vibator" tabindex="-1">What is Vibator? <a class="header-anchor" href="#what-is-vibator" aria-label="Permalink to “What is Vibator?”">​</a></h1><p>Vibator is a quality gate framework for coding agents. It runs configurable, glob-scoped rules over your project and reports findings an agent can act on directly. It targets JavaScript and TypeScript projects, though some rules apply to any file their globs select.</p><h2 id="the-problem-it-solves" tabindex="-1">The problem it solves <a class="header-anchor" href="#the-problem-it-solves" aria-label="Permalink to “The problem it solves”">​</a></h2><p>You tell your agent the standards in a prompt or an <code>AGENTS.md</code>: document every function, keep locales in sync, never call deprecated APIs. The agent applies them while its context is fresh and drifts as the context grows. Linters and type checkers catch some of the fallout, but a class of defects sits outside their reach: a translation key added to one locale only, an env var read but never documented, a generated file never regenerated. Left alone, the agent enforces those with throwaway scripts of its own.</p><p>Vibator turns those standards into checks that run the same way every time. When one fails, the finding restates the standard, so the agent fixes the problem at its source instead of searching the code for it.</p><h2 id="three-fields-per-finding" tabindex="-1">Three fields per finding <a class="header-anchor" href="#three-fields-per-finding" aria-label="Permalink to “Three fields per finding”">​</a></h2><p>Every finding keeps three fields separate:</p><ul><li><code>message</code>: what is wrong.</li><li><code>expected</code>: the standard, stated positively.</li><li><code>fix</code>: the concrete next action.</li></ul><p>You read them as a sentence. A tool runs <code>vibator --reporter json</code> and acts on <code>fix</code> without parsing prose. The JSON report also carries the code snippet around each finding and an absolute path to every guideline, so a consumer never needs to know where the package manager put the package. The full report format is in the <a href="/reference/configuration.html#the-json-reporter">configuration reference</a>.</p><h2 id="the-guideline-arrives-with-the-failure" tabindex="-1">The guideline arrives with the failure <a class="header-anchor" href="#the-guideline-arrives-with-the-failure" aria-label="Permalink to “The guideline arrives with the failure”">​</a></h2><p>Every rule ships a Markdown guideline: what the rule is, the failure it prevents, and what is expected. <code>vibator explain &lt;rule&gt;</code> prints it, and every finding points at it. You can also map your own documents onto rules, so a finding points the agent at your standards as well as the shipped guideline.</p><h2 id="what-it-checks" tabindex="-1">What it checks <a class="header-anchor" href="#what-it-checks" aria-label="Permalink to “What it checks”">​</a></h2><p>The built-in rules cover the defects common in generated code: committed conflict markers, missing TSDoc, filler names like <code>data</code> and <code>tmp</code>, calls into deprecated APIs, locales out of parity, env vars out of sync with their example file, generated files out of date with their source. The <a href="/reference/rule-catalog.html">rule catalog</a> lists every rule, its defaults, and every option it accepts.</p><p>When the built-ins do not cover your standard, add your own. A standard a regular expression can express goes in the <code>banned-patterns</code> options, in plain JSON. Anything structural becomes a rule in plain TypeScript; see <a href="/reference/writing-rules.html">Writing rules</a>.</p><h2 id="no-baselines" tabindex="-1">No baselines <a class="header-anchor" href="#no-baselines" aria-label="Permalink to “No baselines”">​</a></h2><p>There is no suppression file. A recorded violation stops being a violation, and an agent that reads one learns the standard is optional. To adopt Vibator on a codebase with existing violations, scope the run instead: <code>--since origin/main</code> checks new work immediately and old files when they are next touched. A single line can be exempted with <code>// vibator-ignore: &lt;reason&gt;</code>, and the reason is required.</p><h2 id="where-it-fits" tabindex="-1">Where it fits <a class="header-anchor" href="#where-it-fits" aria-label="Permalink to “Where it fits”">​</a></h2><p>Vibator runs alongside your linter, not instead of it. Formatters own formatting and type checkers own types; Vibator checks what those tools structurally cannot see. We recommend a strict stack of Biome, knip, and dependency-cruiser next to it, and <a href="/gate/">the gate</a> sets all four up in one guided run.</p>`,18)]])}var s=e(a,[[`render`,o]]);export{i as __pageData,s as default};